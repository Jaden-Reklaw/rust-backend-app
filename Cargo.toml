[package]
name = "backend-app"
version = "0.1.0"
edition = "2021"

[dependencies]
dotenv = "0.15.0"
rocket = {version = "0.5.1", features = ["json"] }
serde = "1.0.214"

[dependencies.mongodb]
version = "2.2.0"
default-features = false
features = ["sync"]
